"use client";

/*
 * /discover —— 多模态情绪输入页（与 /mood-input 共享同一界面）。
 */

import { MoodInputView } from "@/components/mood-input-view";

export default function DiscoverPage() {
  return <MoodInputView />;
}
